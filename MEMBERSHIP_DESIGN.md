# 会员积分 + 等级 设计方案（待确认）

按你选的方向：**会员等级按「累计消费金额」定级，积分单独用于抵现/兑换**。
本文是落地前的设计，确认后再实现。先看整体，再看每处规则里需要你拍板的点（标了 ⚠️）。

## 一、数据模型（新增 2 张表 + 订单加几列）

```
Member  会员
  id, phone(唯一), name, level, points_balance(当前可用积分),
  total_spent_cents(累计实付，用于定级), created_at, note

PointsLog  积分流水（每次加/减积分都记一条，可追溯）
  id, member_id, change(正负), type(earn赚/redeem抵/refund_reverse退货冲销/adjust手工),
  order_id, note, created_at

SalesOrder 订单加列：
  member_id(本单会员), earned_points(本单赚的积分),
  redeemed_points(本单用掉的积分), redeem_cents(积分抵扣的金额)
```

会员只有一份、4 店共享（和库存一致，中心服务器统一），任意门店都能查到、累计。

## 二、等级规则（按累计消费）

示例阈值（⚠️ 具体金额你定）：

| 等级 | 累计消费门槛 | 积分倍率 | 会员折扣 |
|---|---|---|---|
| 普通 | ¥0 | 1.0× | 无 |
| 银卡 | 满 ¥5,000 | 1.1× | 无 / 98 折 |
| 金卡 | 满 ¥20,000 | 1.2× | 95 折 |
| 钻卡 | 满 ¥50,000 | 1.5× | 92 折 |

- 定级只看 `total_spent_cents`（累计实付），它只增不减地累加。
- ⚠️ **是否保级**：建议「升了不自动降级」——退货虽然会从累计里扣回对应金额，但等级不回退（对顾客更友好、规则简单）。若你要严格"按当前累计实时定级、退货可能掉级"，也能做，告诉我即可。
- ⚠️ 等级权益是只给「积分倍率」，还是再加「会员价/折扣」？上表给了一种组合，可调。

## 三、积分规则

- **获取**：每消费 ¥1 得 1 分 ×（该等级倍率），向下取整。⚠️ 倍率和兑换比例都做成可配置常量。
- **抵现**：100 积分 = ¥1（⚠️ 比例你定），收银时可勾选用积分抵扣，单笔抵扣不超过订单金额、不超过余额。
- **退货冲销**：退货时，按退货金额比例扣回当时赚的积分（`refund_reverse`），并从 `total_spent_cents` 扣回退货金额。若积分已被用掉导致余额不足，余额可暂记为 0（⚠️ 是否允许负分你定，建议不允许、扣到 0 为止）。

## 四、与现有功能的衔接

- **收银页**：加一个"会员"框——扫会员码或输手机号带出会员，显示等级、可用积分、本单可享折扣；结算时可选"用积分抵 X 元"。结算成功后：按规则加积分、累加 `total_spent`、需要时升级、写 PointsLog；若用了积分则扣减并记流水。
- **退货**：在现有按单退货基础上，附带按比例冲销积分、回退累计消费。
- **新增「会员」页**：会员的增删改查、积分手工调整、积分流水查看。
- **订单/账单**：明细里显示会员、本单赚分/抵扣，便于对账。

## 五、接口草图

```
POST   /api/members                 建会员（手机号唯一）
GET    /api/members?keyword=        按手机号/姓名搜
GET    /api/members/{id}            会员详情 + 积分流水
PUT    /api/members/{id}            改资料
POST   /api/members/{id}/points     手工调整积分（店长）
# 收银扩展：SaleCreate 增加 member_id、redeem_points 两个字段
```

## 六、工作量与建议顺序

1. 建模 + 会员 CRUD 接口（小）
2. 收银绑定会员 + 结算时积分/抵现/升级逻辑（中，最核心）
3. 退货积分冲销（小，挂在现有退货上）
4. 前端：会员页 + 收银会员框 + 订单显示（中）

预计比"登录+退货"这轮略大。建议确认上面几个 ⚠️ 后，我分两批做：先「会员+积分获取/抵现」，再「等级权益+退货冲销」。

---

### 需要你拍板的点（汇总）
1. 等级门槛金额（上表是示例）。
2. 退货是否影响等级（建议：扣累计但不降级）。
3. 等级权益：只给积分倍率，还是加会员折扣/会员价。
4. 积分兑换比例（建议 100 分 = ¥1）。
5. 是否允许积分被退货冲到负数（建议否，扣到 0）。
